export default (theme) => ({
  appContainer: {
    backgroundColor: theme.palette.background.main,
    minHeight: '100vh',
    paddingTop: '4rem',
    paddingBottom: '4rem',
    display: 'flex'
  }
})